Commit Reference: b627c97e8bb7c39dbb61b259b224b65dae63d53e
Date: 2026-06-11
Version: 0.5.11
Branch: feature/group-member-leave-ui

Changes:
- Modified src/app.js: Added 'leave' action to POST `/api/control` to support leaving groups cleanly from the REST API.
- Modified tests/app.test.js: Added unit test verifying the POST `/api/control` 'leave' action works and triggers `sonosMock.leaveGroup`. Mocked `leaveGroup` in the main Jest mock definition.
- Modified public/css/style.css: Made the grouped member badge interactive by setting `cursor: pointer`, adding smooth scale/background-color transition, and adding hover styling.
- Modified public/js/app.js: Added data-room and data-coordinator properties to the grouped member badge markup, updated its tooltip to note that clicking leaves the group, and implemented a delegated click event listener that prompts the user for confirmation and triggers group separation.
- Bumped version to 0.5.11 in package.json and documented changes in CHANGES.md.
